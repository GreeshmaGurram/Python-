n = int(input())

for i in range(1, 2*n):
    if i <= n:
        spaces = "  " * (n-i)
        plus = "+ " * (i - 1)
        print(spaces + plus + "#")
    else:
        spaces = "  " * (i-n)
        plus = "+ " * ((2*n)-i-1)
        print(spaces + plus + "#")
